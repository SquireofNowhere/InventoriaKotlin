<?xml version="1.0" encoding="utf-8"?>
<!-- The designed tile (ic_launcher_foreground) covers everything a launcher mask can show, so this
     only ever appears in edge cases (an unmasked preview, an odd mask). Its flat base colour, so
     any such sliver blends in rather than showing a gradient seam. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z" android:fillColor="#8F4AE2"/>
</vector>
