<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inventoria.app">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <!-- Normal permission, granted at install with no runtime prompt. Only lets us hand an alarm
         or timer to the clock app (and skip its confirmation UI); it grants no read access to
         existing alarms or timers, because Android exposes none. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <!-- Todo alarms (TodoAlarmScheduler). Exact alarms need one of these two: SCHEDULE_EXACT_ALARM
         on Android 12 (user-revocable, so the scheduler checks canScheduleExactAlarms and degrades
         to an inexact alarm), USE_EXACT_ALARM from 13 on (granted at install; reserved for apps
         whose core function is alarms or reminders, which a todo alarm is). BOOT_COMPLETED lets the
         process come back up after a restart so the scheduler can re-arm everything from the
         table; see BootReceiver. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18"/>

    <!-- Package visibility (Android 11+): without this, resolveActivity() for the clock intents
         returns null even when a clock app is installed, and the UI would wrongly report that the
         device has none. Launching the intents works regardless; only the capability check needs
         the declaration. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_TIMER"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS"/>
        </intent>
    </queries>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <application
        android:name=".InventoriaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventoria">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Inventoria.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.Inventoria"
            android:exported="false" />

        <service
            android:name=".ui.screens.task.TaskTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Task timing and tracking for inventory management"/>
        </service>

        <!-- Todo alarms fire here (and their Done / Snooze buttons land here). Not exported: every
             PendingIntent aimed at it is created by this app. -->
        <receiver
            android:name=".data.alarm.TodoAlarmReceiver"
            android:exported="false" />

        <!-- Exported because the system sends these; the receiver itself does nothing but exist
             so the process starts and TodoAlarmScheduler re-arms alarms from the table. -->
        <receiver
            android:name=".data.alarm.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            xmlns:tools="http://schemas.android.com/tools"
            tools:node="merge" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:authorities="${applicationId}.fileprovider"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- WorkManager auto-initializes itself via this provider before Application.onCreate()
             runs, using its default (non-Hilt) worker factory, which silently makes
             InventoriaApplication's Configuration.Provider/HiltWorkerFactory setup ineffective,
             since WorkManager.getInstance() then just returns the already-initialized default
             instance. Removing the auto-init meta-data is required for HiltWorker workers
             (SyncWorker) to actually be constructible; without this, WorkManager fails every
             SyncWorker run with NoSuchMethodException since the default factory can't supply its
             Hilt-injected constructor params. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            xmlns:tools="http://schemas.android.com/tools"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>